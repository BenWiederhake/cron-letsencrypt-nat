# Folders are intended for the per-domain secret keys.
# Prevent mistakes by ingoring all folders:
/*/

# Any custom hooks
/local_hook_*

# Also, *definitely* ignore anything that looks like a key.
*.csr
*.key
*.pem
private_key.der
private_key.json
